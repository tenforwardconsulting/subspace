---
all:
  hosts:
    dev1:
      ansible_ssh_host: localhost
      ansible_ssh_user: ubuntu
      ansible_ssh_private_key_file: subspace.pem
      hostname: dev1
  children:
    devservers:
      dev1:
