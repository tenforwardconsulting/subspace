# rails
rails_env: <%= @env %>
database_host: "{{database_host}}"
use_sudo: true

# apache
server_name: "{{ansible_ssh_host}}"

# postgresql
postgresql_authentication:
  - type: local
    user: "{{database_user}}"
    database: 'all'
    method: trust
